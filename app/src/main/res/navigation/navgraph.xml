<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navgraph"
    app:startDestination="@id/musicList">

    <fragment
        android:id="@+id/musicList"
        android:name="com.shruti.musicapp.MusicList"
        android:label="fragment_music_list"
        tools:layout="@layout/fragment_music_list" >
        <action
            android:id="@+id/action_musicList_to_musicPlay"
            app:destination="@id/musicPlay" />
    </fragment>
    <fragment
        android:id="@+id/musicPlay"
        android:name="com.shruti.musicapp.MusicPlay"
        android:label="fragment_music_play"
        tools:layout="@layout/fragment_music_play" />
</navigation>